# Example: connecting mcp-anon with gemini-cli via HTTP

## Start mcp-anon server

```bash
cd integration/gemini-cli/http
export ANON_TARGET=../basic
bash start-mcp-anon-with-http.sh
```

## Then start gemini-cli on another terminal

```bash
gemini
```

